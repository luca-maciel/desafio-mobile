import { View, Text } from "react-native";

export default function LoginPage(){
    return(
        <View style={{flex:1, justifyContent: "center", alignItems:"center"}}>
            <Text>Página do login</Text>
        </View>
    );
}